# Copy this file to `.env` and `.env.production`, then set the URL
# printed by `cloudflared tunnel --url http://localhost:3000`.
# Leave blank in `.env` for local dev (frontend talks to same-origin).
VITE_API_URL=
